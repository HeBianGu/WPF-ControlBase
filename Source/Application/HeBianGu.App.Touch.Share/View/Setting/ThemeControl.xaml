<UserControl x:Class="HeBianGu.App.Touch.View.Setting.ThemeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HeBianGu.App.Touch.View.Setting"
             xmlns:h="QQ:908293466"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBlock" BasedOn="{StaticResource C.TextBlock.Default}"/>

            <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Static h:ComboBoxKeys.Single}}">
                <Setter Property="Width" Value="500"/>
                <Setter Property="Height" Value="{StaticResource C.WindowItemHeight.Default}"/>
                <Setter Property="FontSize" Value="{StaticResource C.FontSize.Default}"/>
                <Setter Property="Margin" Value="45,0"/>
            </Style>

            <Style TargetType="h:FCheckBox" BasedOn="{StaticResource {x:Static h:CheckBoxKeys.Default}}">
                <Setter Property="FontSize" Value="{StaticResource C.FontSize.Default}"/>
                <Setter Property="h:Cattach.FIconSize" Value="{StaticResource C.FontSize.Default}"/>
            </Style>
            
        </Grid.Resources>
        
        <h:ThemeSet Margin="50,20"/>
    </Grid>
</UserControl>
