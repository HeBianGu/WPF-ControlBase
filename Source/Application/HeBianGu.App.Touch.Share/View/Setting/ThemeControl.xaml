<UserControl x:Class="HeBianGu.App.Touch.View.Setting.ThemeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:h="https://github.com/HeBianGu"
             xmlns:local="clr-namespace:HeBianGu.App.Touch.View.Setting"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid>
        <!--<Grid.Resources>
            <Style BasedOn="{StaticResource C.TextBlock.Default}" TargetType="TextBlock" />
            <Style BasedOn="{StaticResource {x:Static h:ComboBoxKeys.Single}}" TargetType="ComboBox">
                <Setter Property="Width" Value="500" />
                --><!--<Setter Property="Height" Value="{StaticResource C.WindowItemHeight.Default}" />--><!--
                <Setter Property="FontSize" Value="{StaticResource C.FontSize.Default}" />
                <Setter Property="Margin" Value="45,0" />
            </Style>
            <Style BasedOn="{StaticResource {x:Static h:CheckBoxKeys.Default}}" TargetType="CheckBox">
                <Setter Property="FontSize" Value="{StaticResource C.FontSize.Default}" />
                <Setter Property="h:Cattach.IconSize" Value="{StaticResource C.FontSize.Default}" />
            </Style>
        </Grid.Resources>-->
        <h:ThemeSet Margin="0,20" HorizontalAlignment="Left" Width="950"/>
    </Grid>
</UserControl>
