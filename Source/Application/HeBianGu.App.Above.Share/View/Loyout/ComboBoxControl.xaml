<UserControl x:Class="HeBianGu.App.Above.View.Loyout.ComboBoxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cl="clr-namespace:System.Collections;assembly=mscorlib"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:h="https://github.com/HeBianGu"
             xmlns:local="clr-namespace:HeBianGu.App.Above.View.Loyout"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <TabControl ItemContainerStyle="{StaticResource {x:Static h:TabItemKeys.Line}}">
        <TabControl.Resources>
            <Style BasedOn="{StaticResource {x:Static h:ComboBoxKeys.Default}}" TargetType="ComboBox">
                <Setter Property="ItemsSource" Value="{StaticResource D.ArrayList.Items}" />
            </Style>
        </TabControl.Resources>
        <TabItem Header="Default">
            <ScrollViewer>
                <UniformGrid Columns="3">
                    <h:Interaction.Triggers>
                        <h:EventTrigger EventName="Loaded">
                            <h:InvokeRandomSplitAnimationAction SplitMilliSecond="-800">
                                <h:InvokeRandomSplitAnimationAction.Timelines>

                                    <h:RandomDoubleAnimation Max="800" Min="-800"
                                                             UseFrom="True"
                                                             UseTo="False"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)"
                                                             To="0"
                                                             Duration="00:00:01">
                                        <DoubleAnimation.EasingFunction>
                                            <BackEase />
                                        </DoubleAnimation.EasingFunction>
                                    </h:RandomDoubleAnimation>
                                    <h:RandomDoubleAnimation Storyboard.TargetProperty="Opacity"
                                                             From="0" To="1"
                                                             Duration="00:00:01" />


                                </h:InvokeRandomSplitAnimationAction.Timelines>
                            </h:InvokeRandomSplitAnimationAction>
                        </h:EventTrigger>
                    </h:Interaction.Triggers>

                    <GroupBox Header="S.ComboBox.Default">
                        <ComboBox h:Cattach.Watermark="请选择你要申报的大学" ItemsSource="{StaticResource D.ArrayList.Items}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="IsEditable='True'">
                        <ComboBox h:Cattach.Watermark="请选择你要申报的大学" IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox IsEditable="True" h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="IsEnabled='False'">
                        <ComboBox IsEnabled="False" ItemsSource="{StaticResource D.ArrayList.Items}" SelectedIndex="3" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox IsEnabled="False" SelectedIndex="3" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.ComboBox.Label">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{DynamicResource {x:Static h:ComboBoxKeys.Label}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox  ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{DynamicResource {x:Static h:ComboBoxKeys.Label}}"  h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="S.ComboBox.Label">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  IsEditable="True"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{DynamicResource {x:Static h:ComboBoxKeys.Label}}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{DynamicResource {x:Static h:ComboBoxKeys.Label}}" h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.ComboBox.Label">
                        <ComboBox h:Cattach.Title="选择类别"
                                  IsEnabled="False"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  SelectedIndex="3"
                                  Style="{DynamicResource {x:Static h:ComboBoxKeys.Label}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEnabled="False" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{DynamicResource {x:Static h:ComboBoxKeys.Label}}" SelectedIndex="3" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.ComboBox.LabelClear">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClear}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox  ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClear}}"  h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.ComboBox.LabelClear">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  IsEditable="True"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClear}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClear}}" h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.ComboBox.LabelClear">
                        <ComboBox h:Cattach.Title="选择类别"
                                  IsEnabled="False"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  SelectedIndex="3"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClear}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox IsEnabled="False" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClear}}" SelectedIndex="3" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.ComboBox.Clear">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.Clear}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别"
                      Style="{StaticResource {x:Static h:ComboBoxKeys.Clear}}"  h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.ComboBox.Clear">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  IsEditable="True"
                                  IsReadOnly="True"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  SelectedIndex="2"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.Clear}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      IsReadOnly="True" SelectedIndex="2"
                      Style="{StaticResource {x:Static h:ComboBoxKeys.Clear}}" h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.ComboBox.Clear">
                        <ComboBox h:Cattach.Title="选择类别"
                                  IsEnabled="False"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  SelectedIndex="3"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.Clear}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEnabled="False" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{StaticResource {x:Static h:ComboBoxKeys.Clear}}" SelectedIndex="3" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="S.Button.MouseOver">
                        <ComboBox h:Cattach.Watermark="请选择你要申报的大学" ItemsSource="{StaticResource D.ArrayList.Items}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox   ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.Button.Close">
                        <ComboBox h:Cattach.Watermark="请选择你要申报的大学" IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.Button.Close.MouseOver">
                        <ComboBox IsEnabled="False" ItemsSource="{StaticResource D.ArrayList.Items}" SelectedIndex="3" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEnabled="False" ItemsSource="{StaticResource D.ArrayList.Items}" SelectedIndex="3" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                </UniformGrid>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="Single">
            <ScrollViewer>
                <UniformGrid VerticalAlignment="Top" Columns="3">
                    <h:Interaction.Triggers>
                        <h:EventTrigger EventName="Loaded">
                            <h:InvokeRandomSplitAnimationAction SplitMilliSecond="-800">
                                <h:InvokeRandomSplitAnimationAction.Timelines>

                                    <!--<h:RandomDoubleAnimation Duration="00:00:01" From="0" To="1"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" From="0" To="1"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" UseFrom="True" Min="0" Max="1440" To="0"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[2].(RotateTransform.Angle)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" UseFrom="True" Min="-720" Max="720" To="0"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)"/>-->

                                    <h:RandomDoubleAnimation Max="800" Min="-800"
                                                             UseFrom="True"
                                                             UseTo="False"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)"
                                                             To="0"
                                                             Duration="00:00:01">
                                        <DoubleAnimation.EasingFunction>
                                            <BounceEase />
                                        </DoubleAnimation.EasingFunction>
                                    </h:RandomDoubleAnimation>

                                    <h:RandomDoubleAnimation Storyboard.TargetProperty="Opacity"
                                                             From="0" To="1"
                                                             Duration="00:00:01" />


                                </h:InvokeRandomSplitAnimationAction.Timelines>
                            </h:InvokeRandomSplitAnimationAction>
                        </h:EventTrigger>
                    </h:Interaction.Triggers>

                    <GroupBox Header="S.ComboBox.Label.Single">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelSingle}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelSingle}}"  h:Cattach.Watermark="请选择你要申报的大学" />
                            ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="IsEditable='True'">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  IsEditable="True"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelSingle}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelSingle}}" h:Cattach.Watermark="请选择你要申报的大学" />
                            ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="IsEnabled='False'">
                        <ComboBox h:Cattach.Title="选择类别"
                                  IsEnabled="False"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  SelectedIndex="3"
                                  Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelSingle}}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEnabled="False" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelSingle}}" SelectedIndex="3" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.ComboBox.Label">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClearSingle}}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClearSingle}}"  h:Cattach.Watermark="请选择你要申报的大学" />

                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="S.ComboBox.Label">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  IsEditable="True"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClearSingle}}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClearSingle}}" h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.ComboBox.Label">
                        <ComboBox h:Cattach.Title="选择类别"
                                  IsEnabled="False"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  SelectedIndex="3"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClearSingle}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEnabled="False" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClearSingle}}" SelectedIndex="3" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                </UniformGrid>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="Accent">
            <ScrollViewer>
                <UniformGrid VerticalAlignment="Top" Columns="3">
                    <h:Interaction.Triggers>
                        <h:EventTrigger EventName="Loaded">
                            <h:InvokeRandomSplitAnimationAction SplitMilliSecond="-800">
                                <h:InvokeRandomSplitAnimationAction.Timelines>

                                    <!--<h:RandomDoubleAnimation Duration="00:00:01" From="0" To="1"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" From="0" To="1"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" UseFrom="True" Min="0" Max="1440" To="0"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[2].(RotateTransform.Angle)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" UseFrom="True" Min="-720" Max="720" To="0"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)"/>-->

                                    <h:RandomDoubleAnimation Max="800" Min="-800"
                                                             UseFrom="True"
                                                             UseTo="False"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)"
                                                             To="0"
                                                             Duration="00:00:01">
                                        <DoubleAnimation.EasingFunction>
                                            <BounceEase />
                                        </DoubleAnimation.EasingFunction>
                                    </h:RandomDoubleAnimation>

                                    <h:RandomDoubleAnimation Storyboard.TargetProperty="Opacity"
                                                             From="0" To="1"
                                                             Duration="00:00:01" />


                                </h:InvokeRandomSplitAnimationAction.Timelines>
                            </h:InvokeRandomSplitAnimationAction>
                        </h:EventTrigger>
                    </h:Interaction.Triggers>

                    <GroupBox Header="h:ComboBoxKeys.LabelAccent">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelAccent}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelSingle}}"  h:Cattach.Watermark="请选择你要申报的大学" />
                            ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="IsEditable='True'">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  IsEditable="True"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelAccent}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelSingle}}" h:Cattach.Watermark="请选择你要申报的大学" />
                            ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="IsEnabled='False'">
                        <ComboBox h:Cattach.Title="选择类别"
                                  IsEnabled="False"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  SelectedIndex="3"
                                  Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelAccent}}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEnabled="False" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{DynamicResource {x:Static h:ComboBoxKeys.LabelSingle}}" SelectedIndex="3" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="h:ComboBoxKeys.LabelClearAccent">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClearAccent}}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClearSingle}}"  h:Cattach.Watermark="请选择你要申报的大学" />

                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="h:ComboBoxKeys.ClearAccent">
                        <ComboBox h:Cattach.Title="选择类别"
                                  h:Cattach.Watermark="请选择你要申报的大学"
                                  IsEditable="True"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.ClearAccent}}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClearSingle}}" h:Cattach.Watermark="请选择你要申报的大学" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="h:ComboBoxKeys.LabelAccent">
                        <ComboBox h:Cattach.Title="选择类别"
                                  IsEnabled="False"
                                  ItemsSource="{StaticResource D.ArrayList.Items}"
                                  SelectedIndex="3"
                                  Style="{StaticResource {x:Static h:ComboBoxKeys.LabelAccent}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <ComboBox    IsEnabled="False" ItemsSource="{StaticResource D.ArrayList.Items}" h:Cattach.Title="选择类别" 
                      Style="{StaticResource {x:Static h:ComboBoxKeys.LabelClearSingle}}" SelectedIndex="3" />
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                </UniformGrid>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="Multi">
            <ScrollViewer>
                <UniformGrid Columns="3">
                    <h:Interaction.Triggers>
                        <h:EventTrigger EventName="Loaded">
                            <h:InvokeRandomSplitAnimationAction SplitMilliSecond="-800">
                                <h:InvokeRandomSplitAnimationAction.Timelines>

                                    <!--<h:RandomDoubleAnimation Duration="00:00:01" From="0" To="1"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" From="0" To="1"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" UseFrom="True" Min="0" Max="1440" To="0"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[2].(RotateTransform.Angle)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" UseFrom="True" Min="-720" Max="720" To="0"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)"/>-->

                                    <h:RandomDoubleAnimation Max="800" Min="-800"
                                                             UseFrom="True"
                                                             UseTo="False"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)"
                                                             To="0"
                                                             Duration="00:00:01" />
                                    <h:RandomDoubleAnimation Storyboard.TargetProperty="Opacity"
                                                             From="0" To="1"
                                                             Duration="00:00:01" />


                                </h:InvokeRandomSplitAnimationAction.Timelines>
                            </h:InvokeRandomSplitAnimationAction>
                        </h:EventTrigger>
                    </h:Interaction.Triggers>

                    <GroupBox Header="S.ComboBox.Default">
                        <h:MultiComboBox h:Cattach.Title="选择类别："
                                         h:Cattach.Watermark="请选择你要申报的大学"
                                         IsEditable="True"
                                         ItemsSource="{StaticResource D.ArrayList.Items}"
                                         SelectedItems="{Binding SelectedItems}"
                                         Style="{StaticResource {x:Static h:MultiComboBox.ClearKey}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <h:MultiComboBox   IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" 
                   h:Cattach.Title="选择类别：" h:Cattach.Watermark="请选择你要申报的大学" Style="{StaticResource {x:Static local:MultiComboBox.ClearKey}}"/>
                            ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="IsEditable='True'">
                        <h:MultiComboBox h:Cattach.Title="选择类别："
                                         h:Cattach.Watermark="请选择你要申报的大学"
                                         IsEditable="True"
                                         ItemsSource="{StaticResource D.ArrayList.Items}"
                                         SelectedItems="{Binding SelectedItems}"
                                         Style="{StaticResource {x:Static h:MultiComboBox.LabelKey}}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <h:MultiComboBox   IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}"
                   h:Cattach.Title="选择类别：" h:Cattach.Watermark="请选择你要申报的大学" Style="{StaticResource {x:Static h:MultiComboBox.LabelKey}}"/>
                            ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="IsEnabled='False'">
                        <h:MultiComboBox h:Cattach.Title="选择类别："
                                         h:Cattach.Watermark="请选择你要申报的大学"
                                         IsEditable="False"
                                         ItemsSource="{StaticResource D.ArrayList.Items}"
                                         SelectedItems="{Binding SelectedItems}"
                                         Style="{StaticResource {x:Static h:MultiComboBox.LabelClearKey}}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <h:MultiComboBox   IsEditable="False" ItemsSource="{StaticResource D.ArrayList.Items}"
                   h:Cattach.Title="选择类别：" h:Cattach.Watermark="请选择你要申报的大学" Style="{StaticResource {x:Static h:MultiComboBox.LabelClearKey}}"/>

                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                    <GroupBox Header="S.ComboBox.Label">
                        <h:MultiComboBox h:Cattach.Title="选择类别："
                                         h:Cattach.Watermark="请选择你要申报的大学"
                                         IsEditable="True"
                                         IsReadOnly="True"
                                         ItemsSource="{StaticResource D.ArrayList.Items}"
                                         SelectedIndex="3"
                                         SelectedItems="{Binding SelectedItems}"
                                         Style="{StaticResource {x:Static h:MultiComboBox.LabelClearKey}}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <h:MultiComboBox   IsEditable="True" ItemsSource="{StaticResource D.ArrayList.Items}" IsReadOnly="True" SelectedIndex="3"
                   h:Cattach.Title="选择类别：" h:Cattach.Watermark="请选择你要申报的大学" Style="{StaticResource {x:Static h:MultiComboBox.LabelClearKey}}"/>
                    ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="S.ComboBox.Label">
                        <h:MultiComboBox h:Cattach.Title="选择类别："
                                         h:Cattach.Watermark="请选择你要申报的大学"
                                         IsEditable="False"
                                         IsEnabled="False"
                                         ItemsSource="{StaticResource D.ArrayList.Items}"
                                         SelectedItems="{Binding SelectedItems}"
                                         Style="{StaticResource {x:Static h:MultiComboBox.LabelClearKey}}" />

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <h:MultiComboBox   IsEditable="False" ItemsSource="{StaticResource D.ArrayList.Items}" IsEnabled="False"
                   h:Cattach.Title="选择类别：" h:Cattach.Watermark="请选择你要申报的大学" Style="{StaticResource {x:Static h:MultiComboBox.LabelClearKey}}"/>
                ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                </UniformGrid>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="Title">
            <ScrollViewer>
                <UniformGrid Columns="3">
                    <h:Interaction.Triggers>
                        <h:EventTrigger EventName="Loaded">
                            <h:InvokeRandomSplitAnimationAction SplitMilliSecond="-800">
                                <h:InvokeRandomSplitAnimationAction.Timelines>

                                    <h:RandomDoubleAnimation Max="800" Min="-800"
                                                             UseFrom="True"
                                                             UseTo="False"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)"
                                                             To="0"
                                                             Duration="00:00:01">
                                        <DoubleAnimation.EasingFunction>
                                            <ElasticEase />
                                        </DoubleAnimation.EasingFunction>
                                    </h:RandomDoubleAnimation>
                                    <h:RandomDoubleAnimation Storyboard.TargetProperty="Opacity"
                                                             From="0" To="1"
                                                             Duration="00:00:01" />

                                </h:InvokeRandomSplitAnimationAction.Timelines>
                            </h:InvokeRandomSplitAnimationAction>
                        </h:EventTrigger>
                    </h:Interaction.Triggers>

                    <GroupBox Header="S.ComboBox.Default">
                        <h:ComboboxTextBox h:Cattach.Watermark="我是水印" SelectedIndex="0" Style="{DynamicResource S.ComboboxTextBox.Clear}">
                            <system:String>姓名</system:String>
                            <system:String>身份证号</system:String>
                            <system:String>护照</system:String>
                            <system:String>学生证号</system:String>
                            <system:String>其他</system:String>
                        </h:ComboboxTextBox>

                        <h:Cattach.Text>
                            <![CDATA[ 
                    <h:ComboboxTextBox  SelectedIndex="0" h:Cattach.Watermark="我是水印" Style="{DynamicResource S.ComboboxTextBox.Clear}">
                        <system:String>姓名</system:String>
                        <system:String>身份证号</system:String>
                        <system:String>护照</system:String>
                        <system:String>学生证号</system:String>
                        <system:String>其他</system:String>
                    </h:ComboboxTextBox>
                            ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                </UniformGrid>
            </ScrollViewer>
        </TabItem>

        <!--<TabItem Header="Filter">
            <ScrollViewer>
                <UniformGrid Columns="3">
                    <h:Interaction.Triggers>
                        <h:EventTrigger EventName="Loaded">
                            <h:InvokeRandomSplitAnimationAction SplitMilliSecond="-800">
                                <h:InvokeRandomSplitAnimationAction.Timelines>

                                    -->
        <!--<h:RandomDoubleAnimation Duration="00:00:01" From="0" To="1"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" From="0" To="1"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" UseFrom="True" Min="0" Max="1440" To="0"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[2].(RotateTransform.Angle)"/>
                                                                            <h:RandomDoubleAnimation Duration="00:00:01" UseFrom="True" Min="-720" Max="720" To="0"  Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)"/>-->
        <!--

                                    <h:RandomDoubleAnimation Max="800" Min="-800"
                                                             UseFrom="True"
                                                             UseTo="False"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)"
                                                             To="0"
                                                             Duration="00:00:01">
                                        <DoubleAnimation.EasingFunction>
                                            <BounceEase />
                                        </DoubleAnimation.EasingFunction>
                                    </h:RandomDoubleAnimation>
                                    <h:RandomDoubleAnimation Storyboard.TargetProperty="Opacity"
                                                             From="0" To="1"
                                                             Duration="00:00:01" />


                                </h:InvokeRandomSplitAnimationAction.Timelines>
                            </h:InvokeRandomSplitAnimationAction>
                        </h:EventTrigger>
                    </h:Interaction.Triggers>

                    <GroupBox Header="S.ComboBox.Default">
                        <h:SearchComboBox HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          h:Cattach.Watermark="请选择你要申报的大学"
                                          BindingItems="{Binding ComboBoxItems, Mode=TwoWay}"
                                          DeleteMode="Delete"
                                          IsEditable="False"
                                          SearchMode="TextChanged"
                                          SelectedValuePath="Value" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <h:SearchComboBox VerticalAlignment="Center" HorizontalAlignment="Center" h:Cattach.Watermark="请选择你要申报的大学"
                                           SearchMode="TextChanged" DeleteMode="Delete" IsEditable="False" 
                                           BindingItems="{Binding ComboBoxItems, Mode=TwoWay}" SelectedValuePath="Value">
                    </h:SearchComboBox>
                            ]]>
                        </h:Cattach.Text>
                    </GroupBox>

                    <GroupBox Header="IsEditable='True'">
                        <h:SearchComboBox HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          h:Cattach.Title="选择项目："
                                          h:Cattach.Watermark="请选择你要申报的大学"
                                          BindingItems="{Binding ComboBoxItems, Mode=TwoWay}"
                                          DeleteMode="Delete"
                                          IsEditable="False"
                                          SearchMode="TextChanged"
                                          SelectedValuePath="Value"
                                          Style="{DynamicResource S.FComboBox.Single.Label}" />
                        <h:Cattach.Text>
                            <![CDATA[ 
                    <h:SearchComboBox VerticalAlignment="Center" HorizontalAlignment="Center" h:Cattach.Watermark="请选择你要申报的大学"
                                           SearchMode="TextChanged" DeleteMode="Delete" IsEditable="False" Style="{DynamicResource S.FComboBox.Single.Label}" h:Cattach.Title="选择项目："
                                           BindingItems="{Binding ComboBoxItems, Mode=TwoWay}" SelectedValuePath="Value">
                    </h:SearchComboBox>
                            ]]>
                        </h:Cattach.Text>
                    </GroupBox>
                </UniformGrid>
            </ScrollViewer>
        </TabItem>-->
    </TabControl>

</UserControl>
