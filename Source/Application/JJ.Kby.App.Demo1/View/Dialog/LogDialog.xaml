<UserControl x:Class="JJ.Kby.App.Demo1.View.Dialog.LogDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JJ.Kby.App.Demo1.View.Dialog" xmlns:h="QQ:908293466"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Width="800" Height="400">
    <GroupBox Header="运行日志"> 
        <ListBox ItemsSource="{Binding LogMessages}" Style="{StaticResource S.ListBox.Clear}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel  LastChildFill="False">
                        <TextBlock Text="{Binding Value}" FontWeight="Bold" Foreground="{DynamicResource S.Brush.Green.Notice}"/>
                        <!--<TextBlock Text="{Binding Value2}"/>-->
                        <TextBlock Text="{Binding Value3}" Margin="50,0"/>
                        <TextBlock Text="{Binding Value4}"/>
                        <TextBlock Text="{Binding Value2}" Margin="10,0" DockPanel.Dock="Right"/>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <h:Cattach.AttachContent>
            <ControlTemplate>
                <h:FButton  Width="35" Height="35" 
                                        MouseOverForeground="{DynamicResource S.Brush.TextForeground.White}"  PressedForeground="{DynamicResource S.Brush.TextForeground.White}"
                                        AllowsAnimation="True" 
                                        Style="{DynamicResource S.FButton.Style.Transparent}"  
                                        Icon="&#xe6f4;" FIconSize="15" Command="{x:Static h:CommandService.MessageLayerCloseCommand}"
                                        Foreground="{DynamicResource S.Brush.TextForeground.White}">
                </h:FButton>
            </ControlTemplate>
        </h:Cattach.AttachContent>
    </GroupBox>
</UserControl>
