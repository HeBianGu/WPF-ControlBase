<UserControl x:Class="HeBianGu.App.Manager.View.Attach.RandomControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cl="clr-namespace:System.Collections;assembly=mscorlib"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:h="https://github.com/HeBianGu"
             xmlns:local="clr-namespace:HeBianGu.App.Manager.View.Attach"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel>
            <GroupBox Height="900" Header="ScaleAction">
                <DockPanel>
                    <h:SwitchToggleButton x:Name="cb1"
                                          Margin="5"
                                          HorizontalAlignment="Left"
                                          BorderCornerRadius="3"
                                          Content="显示/隐藏"
                                          CornerRadius="2"
                                          DockPanel.Dock="Top"
                                          IsChecked="True" />

                    <UniformGrid Columns="1">
                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb1, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:ScaleTransition EndX="{h:IntRandowm From=0,
                                                                           To=10}"
                                                       EndY="{h:IntRandowm From=0,
                                                                           To=10}"
                                                       StartX="{h:IntRandowm From=0,
                                                                             To=10}"
                                                       StartY="{h:IntRandowm From=0,
                                                                             To=10}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb1, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:ScaleTransition EndX="{h:IntRandowm From=0,
                                                                           To=10}"
                                                       EndY="{h:IntRandowm From=0,
                                                                           To=10}"
                                                       StartX="{h:IntRandowm From=0,
                                                                             To=10}"
                                                       StartY="{h:IntRandowm From=0,
                                                                             To=10}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb1, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:ScaleTransition EndX="{h:IntRandowm From=0,
                                                                           To=10}"
                                                       EndY="{h:IntRandowm From=0,
                                                                           To=10}"
                                                       StartX="{h:IntRandowm From=0,
                                                                             To=10}"
                                                       StartY="{h:IntRandowm From=0,
                                                                             To=10}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb1, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:ScaleTransition EndX="{h:IntRandowm From=0,
                                                                           To=10}"
                                                       EndY="{h:IntRandowm From=0,
                                                                           To=10}"
                                                       StartX="{h:IntRandowm From=0,
                                                                             To=10}"
                                                       StartY="{h:IntRandowm From=0,
                                                                             To=10}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                    </UniformGrid>


                </DockPanel>
            </GroupBox>

            <GroupBox Height="900" Header="OpacityAction">
                <DockPanel>
                    <h:SwitchToggleButton x:Name="cb2"
                                          Margin="5"
                                          HorizontalAlignment="Left"
                                          BorderCornerRadius="3"
                                          Content="显示/隐藏"
                                          CornerRadius="2"
                                          DockPanel.Dock="Top"
                                          IsChecked="True" />

                    <UniformGrid Columns="1">
                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb2, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:OpacityTransition EndOpacity="{h:DoubleRandowm From=0, To=1}" StartOpacity="{h:DoubleRandowm From=0, To=1}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb2, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:OpacityTransition EndOpacity="{h:DoubleRandowm From=0, To=1}" StartOpacity="{h:DoubleRandowm From=0, To=1}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb2, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:OpacityTransition EndOpacity="{h:DoubleRandowm From=0, To=1}" StartOpacity="{h:DoubleRandowm From=0, To=1}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb2, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:OpacityTransition EndOpacity="{h:DoubleRandowm From=0, To=1}" StartOpacity="{h:DoubleRandowm From=0, To=1}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>
                    </UniformGrid>
                </DockPanel>
            </GroupBox>

            <GroupBox Height="900" Header="TranslateAction">
                <DockPanel>
                    <h:SwitchToggleButton x:Name="cb3"
                                          Margin="5"
                                          HorizontalAlignment="Left"
                                          BorderCornerRadius="3"
                                          Content="显示/隐藏"
                                          CornerRadius="2"
                                          DockPanel.Dock="Top"
                                          IsChecked="True" />

                    <UniformGrid Columns="1">
                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb3, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:TranslateTransition StartPoint="{h:PointRandom From={h:Point X=-500, Y=-500}, To={h:Point X=500, Y=500}}" EndPoint="{h:PointRandom From={h:Point X=-500, Y=-500}, To={h:Point X=500, Y=500}}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb3, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:TranslateTransition StartPoint="{h:PointRandom From={h:Point X=-500, Y=-500}, To={h:Point X=500, Y=500}}" EndPoint="{h:PointRandom From={h:Point X=-500, Y=-500}, To={h:Point X=500, Y=500}}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb3, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:TranslateTransition StartPoint="{h:PointRandom From={h:Point X=-500, Y=-500}, To={h:Point X=500, Y=500}}" EndPoint="{h:PointRandom From={h:Point X=-500, Y=-500}, To={h:Point X=500, Y=500}}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb3, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:TranslateTransition StartPoint="{h:PointRandom From={h:Point X=-500, Y=-500}, To={h:Point X=500, Y=500}}" EndPoint="{h:PointRandom From={h:Point X=-500, Y=-500}, To={h:Point X=500, Y=500}}" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>
                    </UniformGrid>
                </DockPanel>
            </GroupBox>

            <GroupBox Height="900" Header="AngleAction">
                <DockPanel>
                    <h:SwitchToggleButton x:Name="cb8"
                                          Margin="5"
                                          HorizontalAlignment="Left"
                                          BorderCornerRadius="3"
                                          Content="显示/隐藏"
                                          CornerRadius="2"
                                          DockPanel.Dock="Top"
                                          IsChecked="True" />

                    <UniformGrid Columns="1">

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb8, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:AngleTransition EndAngle="{h:IntRandowm From=-720,
                                                                               To=720}"
                                                       RenderTransformOrigin="0.5,0.5"
                                                       StartAngle="{h:IntRandowm From=-720,
                                                                                 To=720}"
                                                       VisibleDuration="500" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>


                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb8, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:AngleTransition EndAngle="{h:IntRandowm From=-720,
                                                                               To=720}"
                                                       RenderTransformOrigin="0.5,0.5"
                                                       StartAngle="{h:IntRandowm From=-720,
                                                                                 To=720}"
                                                       VisibleDuration="500" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>


                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb8, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:AngleTransition EndAngle="{h:IntRandowm From=-720,
                                                                               To=720}"
                                                       RenderTransformOrigin="0.5,0.5"
                                                       StartAngle="{h:IntRandowm From=-720,
                                                                                 To=720}"
                                                       VisibleDuration="500" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>


                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb8, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:AngleTransition EndAngle="{h:IntRandowm From=-720,
                                                                               To=720}"
                                                       RenderTransformOrigin="0.5,0.5"
                                                       StartAngle="{h:IntRandowm From=-720,
                                                                                 To=720}"
                                                       VisibleDuration="500" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>
                    </UniformGrid>
                </DockPanel>
            </GroupBox>

            <GroupBox Height="900" Header="SkewAction">
                <DockPanel>
                    <h:SwitchToggleButton x:Name="cb6"
                                          Margin="5"
                                          HorizontalAlignment="Left"
                                          BorderCornerRadius="3"
                                          Content="显示/隐藏"
                                          CornerRadius="2"
                                          DockPanel.Dock="Top"
                                          IsChecked="True" />

                    <UniformGrid Columns="1">
                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb6, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:SkewTransition EndAngleX="{h:DoubleRandowm From=0,
                                                                                  To=90}"
                                                      EndAngleY="0"
                                                      StartAngleX="{h:DoubleRandowm From=0,
                                                                                    To=90}"
                                                      StartAngleY="0" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb6, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:SkewTransition EndAngleX="{h:DoubleRandowm From=0,
                                                                                  To=90}"
                                                      EndAngleY="0"
                                                      StartAngleX="{h:DoubleRandowm From=0,
                                                                                    To=90}"
                                                      StartAngleY="0" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb6, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:SkewTransition EndAngleX="{h:DoubleRandowm From=0,
                                                                                  To=90}"
                                                      EndAngleY="0"
                                                      StartAngleX="{h:DoubleRandowm From=0,
                                                                                    To=90}"
                                                      StartAngleY="0" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb6, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:SkewTransition EndAngleX="{h:DoubleRandowm From=0,
                                                                                  To=90}"
                                                      EndAngleY="0"
                                                      StartAngleX="{h:DoubleRandowm From=0,
                                                                                    To=90}"
                                                      StartAngleY="0" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>
                    </UniformGrid>
                </DockPanel>
            </GroupBox>

            <GroupBox Height="900" Header="LinearOpacityMaskAction">
                <DockPanel>
                    <h:SwitchToggleButton x:Name="cb7"
                                          Margin="5"
                                          HorizontalAlignment="Left"
                                          BorderCornerRadius="3"
                                          Content="显示/隐藏"
                                          CornerRadius="2"
                                          DockPanel.Dock="Top"
                                          IsChecked="True" />

                    <UniformGrid Columns="1">
                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb7, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:LinearOpacityMaskTransition Viewport="{h:RectRandom From={h:RectMarkup X=0, Y=0, Width=0, Height=0}, To={h:RectMarkup X=1, Y=1, Width=1, Height=1}}" StartPoint="0,1" EndPoint="1,1" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb7, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:LinearOpacityMaskTransition Viewport="{h:RectRandom From={h:RectMarkup X=0, Y=0, Width=0, Height=0}, To={h:RectMarkup X=1, Y=1, Width=1, Height=1}}" StartPoint="0,1" EndPoint="1,1" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb7, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:LinearOpacityMaskTransition Viewport="{h:RectRandom From={h:RectMarkup X=0, Y=0, Width=0, Height=0}, To={h:RectMarkup X=1, Y=1, Width=1, Height=1}}" StartPoint="0,1" EndPoint="1,1" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                        <Grid Margin="5" h:TransitionService.IsVisible="{Binding ElementName=cb7, Path=IsChecked}" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}">
                            <h:TransitionService.VisibleActions>
                                <h:TransitionCollection>
                                    <h:LinearOpacityMaskTransition Viewport="{h:RectRandom From={h:RectMarkup X=0, Y=0, Width=0, Height=0}, To={h:RectMarkup X=1, Y=1, Width=1, Height=1}}" StartPoint="0,1" EndPoint="1,1" />
                                </h:TransitionCollection>
                            </h:TransitionService.VisibleActions>

                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="100"
                                       Text="Grid" />
                        </Grid>

                    </UniformGrid>
                </DockPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
