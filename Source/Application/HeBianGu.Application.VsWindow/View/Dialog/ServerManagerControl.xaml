<UserControl x:Class="HeBianGu.Application.VsWindow.View.Dialog.ServerManagerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HeBianGu.Application.VsWindow.View.Dialog" xmlns:h="QQ:908293466" xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" Background="{DynamicResource S.Brush.TextBackground.Default}"
             d:DesignHeight="450" d:DesignWidth="800" Width="1200" Height="700">
    <UserControl.Resources>
        <x:Array x:Key="S.Array.Users" Type="{x:Type h:TestViewModel}">
            <h:TestViewModel Value="主机一" Value1="(253,569)" Value2="192.168.0.1" Value3="普通用户" Selected="True" DateTime1="{x:Static system:DateTime.Now}"/>
            <h:TestViewModel Value="主机二" Value1="(253,569)" Value2="192.168.0.2" Value3="普通用户" Selected="True" DateTime1="{x:Static system:DateTime.Now}"/>
            <h:TestViewModel Value="主机三" Value1="(253,569)" Value2="192.168.0.3" Value3="普通用户" Selected="False" DateTime1="{x:Static system:DateTime.Now}"/>
            <h:TestViewModel Value="主机四" Value1="(253,569)" Value2="192.168.0.4" Value3="普通用户" Selected="True" DateTime1="{x:Static system:DateTime.Now}"/>
            <h:TestViewModel Value="主机五" Value1="(253,569)" Value2="192.168.0.5" Value3="普通用户" Selected="False" DateTime1="{x:Static system:DateTime.Now}"/>
            <h:TestViewModel Value="主机六" Value1="(253,569)" Value2="192.168.0.6" Value3="普通用户" Selected="True" DateTime1="{x:Static system:DateTime.Now}"/>
        </x:Array>
    </UserControl.Resources>
    <Grid Margin="50,50,50,10">
        <Grid>
            <h:PagedDataGrid PageCountVisible="Collapsed" PageCount="17" BorderThickness="0" Style="{StaticResource S.PagedDataGrid.Default}" DataSource="{StaticResource S.Array.Users}">
                <h:PagedDataGrid.Columns>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.Header>
                            <CheckBox Content="全选"/>
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox Foreground="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow},Path=Foreground}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="名称" Binding="{Binding Value}" Width="*"/>
                    <DataGridTextColumn Header="坐标" Binding="{Binding Value1}" Width="*"/>
                    
                    <DataGridTemplateColumn  Width="2*">
                        <DataGridTemplateColumn.Header>
                            <TextBlock Text="连接状态"/>
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <h:Ping IP="{Binding Value2}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <DataGridTemplateColumn  Width="*">
                        <DataGridTemplateColumn.Header>
                            <TextBlock Text="是否启用"/>
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Selected}" Foreground="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow},Path=Foreground}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!--<DataGridTextColumn Header="创建时间" Binding="{Binding DateTime1}" Width="1.5*"/>-->

                    <DataGridTemplateColumn  Width="1.5*">
                        <DataGridTemplateColumn.Header>
                            <TextBlock Text="操作"/>
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <DockPanel>
                                    <Button Content="删除" Width="Auto" Margin="0,2" Background="{DynamicResource S.Brush.Red.Notice}"/>
                                    <Button Content="编辑" Width="Auto" Margin="2,2" Background="{DynamicResource S.Brush.Green.Notice}"/>
                                    <Button Content="测试连接" Width="Auto" Margin="2,2" Background="{DynamicResource S.Brush.Orange.Notice}"/>
                                </DockPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </h:PagedDataGrid.Columns>

                <h:PagedDataGrid.HeaderContent>
                    <DockPanel VerticalAlignment="Top" LastChildFill="False" Margin="0,5">
                        <Button Content="创建"/>

                        <Button Content="删除" Margin="10,0"/>

                        <Button Content="测试连接"/>
                    </DockPanel>
                </h:PagedDataGrid.HeaderContent>
            </h:PagedDataGrid>
        </Grid>

        <h:FButton Grid.Row="0" Style="{DynamicResource S.FButton.Close}" Grid.ColumnSpan="2"
                IsCancel="True" Command="{x:Static h:CommandService.MessageLayerCloseCommand}" Margin="-40,-20"
                HorizontalAlignment="Right">
            <h:FButton.CommandParameter>
                <system:Boolean>False</system:Boolean>
            </h:FButton.CommandParameter>
        </h:FButton>

        <TextBlock Text="主机管理" VerticalAlignment="Top" Style="{DynamicResource S.TextBlock.Default}" HorizontalAlignment="Left" FontSize="25" Margin="0,-40,0,0" Foreground="{DynamicResource S.Brush.TextForeground.Title}"/>
    </Grid>
</UserControl>
