<UserControl x:Class="HeBianGu.Applications.ControlBase.LinkWindow.View.Loyout.ListBoxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:collections="clr-namespace:System.Collections;assembly=mscorlib"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:h="QQ:908293466"
             xmlns:local="clr-namespace:HeBianGu.Applications.ControlBase.LinkWindow.View.Loyout"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             d:DesignHeight="1450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <ScrollViewer>
        <UniformGrid>
            <GroupBox Header="默认样式">
                <StackPanel>
                    <ListBox ItemsSource="{DynamicResource D.ArrayList.Items}" SelectedIndex="0" Style="{DynamicResource {x:Static h:ListBoxKeys.Default}}" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="精简样式">
                <StackPanel>
                    <ListBox ItemsSource="{DynamicResource D.ArrayList.Items}" SelectedIndex="0" Style="{DynamicResource {x:Static h:ListBoxKeys.Single}}" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="单选样式一">
                <StackPanel>
                    <StackPanel.Resources>
                        <DataTemplate DataType="{x:Type system:String}">
                            <Grid Width="150" Height="{DynamicResource {x:Static h:SystemKeys.ItemHeight}}" Margin="2">
                                <Border x:Name="border"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                                        BorderThickness="1"
                                        CornerRadius="{DynamicResource {x:Static h:CornerRadiusKeys.CornerRadius}}" />
                                <TextBlock x:Name="txt"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource {x:Static h:BrushKeys.ForegroundDefault}}"
                                           Text="{Binding}" />
                            </Grid>

                            <DataTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static h:BrushKeys.AccentMouseOverForeground}}" />
                                    <Setter TargetName="txt" Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.AccentMouseOverForeground}}" />
                                </Trigger>

                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="true">
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static h:BrushKeys.Accent}}" />
                                    <Setter TargetName="txt" Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.Accent}}" />
                                    <Setter TargetName="txt" Property="FontWeight" Value="Black" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </StackPanel.Resources>
                    <ListBox ItemsSource="{DynamicResource D.ArrayList.Items}" Style="{DynamicResource {x:Static h:ListBoxKeys.Single}}" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="单选样式二">
                <StackPanel>
                    <StackPanel.Resources>
                        <DataTemplate DataType="{x:Type system:String}">
                            <Grid Width="150" Height="{DynamicResource {x:Static h:SystemKeys.ItemHeight}}" Margin="2">
                                <Border x:Name="border"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                                        BorderThickness="1"
                                        CornerRadius="{DynamicResource {x:Static h:CornerRadiusKeys.CornerRadius}}" />
                                <TextBlock x:Name="txt"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource {x:Static h:BrushKeys.ForegroundDefault}}"
                                           Text="{Binding}" />
                            </Grid>

                            <DataTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource {x:Static h:BrushKeys.AccentMouseOverBackground}}" />
                                </Trigger>

                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="true">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource {x:Static h:BrushKeys.Accent}}" />
                                    <Setter TargetName="txt" Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.ForegroundWhite}}" />
                                    <Setter TargetName="txt" Property="FontWeight" Value="Black" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </StackPanel.Resources>
                    <ListBox ItemsSource="{DynamicResource D.ArrayList.Items}" Style="{DynamicResource {x:Static h:ListBoxKeys.Single}}" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="单选样式三">
                <StackPanel>
                    <StackPanel.Resources>
                        <DataTemplate DataType="{x:Type system:String}">
                            <Grid Width="150" Height="{DynamicResource {x:Static h:SystemKeys.ItemHeight}}">
                                <Border x:Name="border" Background="Transparent" />
                                <TextBlock x:Name="txt"
                                           Margin="15,0,0,0"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource {x:Static h:BrushKeys.ForegroundDefault}}"
                                           Text="{Binding}" />
                                <Border x:Name="left"
                                        Width="5"
                                        HorizontalAlignment="Left"
                                        Background="{DynamicResource {x:Static h:BrushKeys.Accent}}"
                                        Visibility="Collapsed" />
                            </Grid>
                            <DataTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="txt" Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.AccentMouseOverForeground}}" />
                                </Trigger>

                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="true">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource {x:Static h:BrushKeys.Dark2}}" />
                                    <Setter TargetName="txt" Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.Accent}}" />
                                    <Setter TargetName="left" Property="Visibility" Value="Visible" />
                                    <Setter TargetName="txt" Property="FontWeight" Value="Bold" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </StackPanel.Resources>
                    <ListBox ItemsSource="{DynamicResource D.ArrayList.Items}" SelectedIndex="0" Style="{DynamicResource {x:Static h:ListBoxKeys.Single}}" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="单选样式四">
                <StackPanel>
                    <StackPanel.Resources>
                        <DataTemplate DataType="{x:Type system:String}">
                            <Grid Width="150" Height="{DynamicResource {x:Static h:SystemKeys.ItemHeight}}" Margin="1">
                                <RadioButton GroupName="listbox" IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Style="{StaticResource {x:Static h:RadioButtonKeys.Box}}">
                                    <RadioButton.Content>
                                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding}" />
                                    </RadioButton.Content>
                                </RadioButton>
                            </Grid>
                        </DataTemplate>
                    </StackPanel.Resources>
                    <ListBox ItemsSource="{DynamicResource D.ArrayList.Items}" SelectedIndex="0" Style="{DynamicResource {x:Static h:ListBoxKeys.Single}}" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="单选布局">
                <StackPanel>
                    <StackPanel.Resources>
                        <DataTemplate DataType="{x:Type system:String}">
                            <Grid Width="150" Height="{DynamicResource {x:Static h:SystemKeys.ItemHeight}}" Margin="2">
                                <Border x:Name="border"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                                        BorderThickness="1"
                                        CornerRadius="{DynamicResource {x:Static h:CornerRadiusKeys.CornerRadius}}" />
                                <TextBlock x:Name="txt"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource {x:Static h:BrushKeys.ForegroundDefault}}"
                                           Text="{Binding}" />
                            </Grid>

                            <DataTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static h:BrushKeys.AccentMouseOverForeground}}" />
                                    <Setter TargetName="txt" Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.AccentMouseOverForeground}}" />
                                </Trigger>

                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="true">
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static h:BrushKeys.Accent}}" />
                                    <Setter TargetName="txt" Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.Accent}}" />
                                    <Setter TargetName="txt" Property="FontWeight" Value="Black" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </StackPanel.Resources>
                    <ListBox ItemsSource="{DynamicResource D.ArrayList.Items}" SelectedIndex="0" Style="{DynamicResource {x:Static h:ListBoxKeys.Single}}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="单选布局">
                <StackPanel>
                    <StackPanel.Resources>
                        <DataTemplate DataType="{x:Type system:String}">
                            <Grid Width="120" Height="{DynamicResource {x:Static h:SystemKeys.ItemHeight}}" Margin="2">
                                <Border x:Name="border"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                                        BorderThickness="1" />
                                <TextBlock x:Name="txt"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource {x:Static h:BrushKeys.ForegroundDefault}}"
                                           Text="{Binding}" />
                            </Grid>

                            <DataTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static h:BrushKeys.AccentMouseOverForeground}}" />
                                    <Setter TargetName="txt" Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.AccentMouseOverForeground}}" />
                                </Trigger>

                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="true">
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static h:BrushKeys.Accent}}" />
                                    <Setter TargetName="txt" Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.Accent}}" />
                                    <Setter TargetName="txt" Property="FontWeight" Value="Black" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </StackPanel.Resources>
                    <ListBox ItemsSource="{DynamicResource D.ArrayList.Items}" SelectedIndex="0" Style="{DynamicResource {x:Static h:ListBoxKeys.Single}}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="多选样式">
                <StackPanel>
                    <StackPanel.Resources>
                        <DataTemplate DataType="{x:Type system:String}">
                            <Grid Width="{DynamicResource {x:Static h:SystemKeys.ItemHeight}}" Height="{DynamicResource {x:Static h:SystemKeys.ItemHeight}}">
                                <Border x:Name="border"
                                        Margin="1"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                                        BorderThickness="1" />
                                <TextBlock x:Name="txt"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Text="{Binding}" />
                            </Grid>
                        </DataTemplate>
                    </StackPanel.Resources>
                    <ListBox>
                        <ListBox.ItemsSource>
                            <collections:ArrayList>
                                <system:String>1</system:String>
                                <system:String>2</system:String>
                                <system:String>3</system:String>
                                <system:String>4</system:String>
                                <system:String>5</system:String>
                                <system:String>6</system:String>
                                <system:String>7</system:String>
                                <system:String>8</system:String>
                                <system:String>9</system:String>
                                <system:String>10</system:String>
                            </collections:ArrayList>
                        </ListBox.ItemsSource>
                    </ListBox>
                </StackPanel>
            </GroupBox>
        </UniformGrid>
    </ScrollViewer>
</UserControl>
