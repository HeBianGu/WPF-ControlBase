<UserControl x:Class="HeBianGu.Applications.ControlBase.LinkWindow.SettingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:h="https://github.com/HeBianGu"
             mc:Ignorable="d" Background="{DynamicResource {x:Static h:BrushKeys.BackgroundDefault}}"
            
             d:DesignHeight="450" d:DesignWidth="800" Width="900" Height="500">
    <UserControl.Resources>
        <Style x:Key="C.TextBlock.Default"  TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="10,0"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.ForegroundDefault}}"/>
        </Style>

        <Style x:Key="C.TextBlock.FontSize.18" TargetType="TextBlock" BasedOn="{StaticResource C.TextBlock.Default}">
            <Setter Property="FontSize" Value="18"/>
        </Style>
    </UserControl.Resources>

    <Grid  TextBlock.Foreground="{DynamicResource {x:Static h:BrushKeys.ForegroundDefault}}" TextBlock.FontSize="18" Margin="50,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

        <TextBlock Text="数据导入配置" Style="{StaticResource C.TextBlock.FontSize.18}" HorizontalAlignment="Left" FontWeight="Bold"/>

            <Grid Grid.Row="1">
                <Grid.Resources>
                <Style TargetType="TextBlock" BasedOn="{StaticResource C.TextBlock.Default}">
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>


                <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Static h:TextBoxKeys.Dynamic}}">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Margin" Value="10,0"/>
                    <Setter Property="CaretBrush" Value="{DynamicResource {x:Static h:BrushKeys.ForegroundDefault}}"/>
                    <Setter Property="h:Cattach.IconSize" Value="18"/>
                    <Setter Property="h:Cattach.IconMargin" Value="5"/>
                </Style>
                </Grid.Resources>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.7*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" >
                <Run>数据库地址</Run>
                <Run Foreground="{DynamicResource {x:Static h:BrushKeys.Red}}">*</Run>
                <Run>：</Run></TextBlock>

            <TextBlock Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left">
                <Run>数据库端口</Run>
                <Run Foreground="{DynamicResource {x:Static h:BrushKeys.Red}}">*</Run>
                <Run>：</Run></TextBlock>

            <TextBlock Grid.Row="2" Grid.Column="0" HorizontalAlignment="Left">
                <Run>数据库名称</Run>
                <Run Foreground="{DynamicResource {x:Static h:BrushKeys.Red}}">*</Run>
                <Run>：</Run></TextBlock>

            <TextBlock Grid.Row="3" Grid.Column="0" HorizontalAlignment="Left">
                <Run>数据库登录名</Run>
                <Run Foreground="{DynamicResource {x:Static h:BrushKeys.Red}}">*</Run>
                <Run>：</Run></TextBlock>

            <TextBlock Grid.Row="4" Grid.Column="0" HorizontalAlignment="Left">
                <Run>数据库密码</Run>
                <Run Foreground="{DynamicResource {x:Static h:BrushKeys.Red}}">*</Run>
                <Run>：</Run></TextBlock>

            <TextBlock Grid.Row="5" Grid.Column="0" HorizontalAlignment="Left">
                <Run>检验报告类型</Run>
                <Run Foreground="{DynamicResource {x:Static h:BrushKeys.Red}}"></Run>
                <Run>：</Run></TextBlock>

            <TextBlock Grid.Row="6" Grid.Column="0" HorizontalAlignment="Left">
                <Run>指引单编号</Run>
                <Run Foreground="{DynamicResource {x:Static h:BrushKeys.Red}}"></Run>
                <Run>：</Run></TextBlock>


            <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                            h:Cattach.Icon="&#xe68c;" Text="{Binding SettingViewModel.IP}"
                            h:Cattach.Watermark="请输入数据库地址"/>

            <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                            h:Cattach.Icon="&#xe763;"  Text="{Binding SettingViewModel.Port}"
                            h:Cattach.Watermark="请输入数据库端口"/>

            <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                            h:Cattach.Icon="&#xe787;"  Text="{Binding SettingViewModel.Name}"
                            h:Cattach.Watermark="请输入数据库名称"/>

            <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2"
                            h:Cattach.Icon="&#xea92;"  Text="{Binding SettingViewModel.User}"
                            h:Cattach.Watermark="请输入数据库登录名称"/>

            <h:PasswordBox x:Name="bpw_bindpassword"  Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2"
                             h:Cattach.Watermark="请输入数据库登录密码" 
                             Style="{DynamicResource {x:Static h:PasswordBoxKeys.Default}}" Margin="10,0"
                            FontSize="18" Width="Auto" Height="38"  
                            h:Cattach.Icon="&#xe780;" 
                            h:Cattach.IconSize="18" 
                            h:Cattach.IconMargin="5" 
                            PassWord="{Binding SettingViewModel.PSD,Mode=TwoWay}"  />

            <TextBox Grid.Row="5" Grid.Column="1" 
                            h:Cattach.Icon="&#xe6f3;" IsEnabled="False"
                            h:Cattach.Watermark="请输入报告类型开始时间"/>

            <TextBox Grid.Row="5" Grid.Column="2" 
                            h:Cattach.Icon="&#xe6f3;" IsEnabled="False"
                            h:Cattach.Watermark="请输入报告类型结束时间"/>

            <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" IsEnabled="False"
                            h:Cattach.Icon="&#xe720;"
                            h:Cattach.Watermark="指引单编号"/>

            </Grid>

            <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                
            <Button Content="取消" Margin="0,0,50,0" Style="{DynamicResource {x:Static h:ButtonKeys.Transparent}}" 
                               BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}" 
                               BorderThickness="1" Width="120"
                               Command="{Binding RelayCommand}" CommandParameter="SetCancel"/>
                
            <Button Content="确认" Command="{Binding RelayCommand}" CommandParameter="SetSumit"/>
            </StackPanel>


        <Button Style="{DynamicResource {x:Static h:ButtonKeys.Transparent}}" 
                       h:Cattach.Icon="&#xe72c;"  Click="Button_Click" h:Cattach.AllowsAnimation="True"
                       h:Cattach.MouseOverForeground="{DynamicResource {x:Static h:BrushKeys.Red}}"
                       Width="30" Height="30"
                       h:Cattach.IconSize="14"  Margin="0,-10,-45,0"
                       HorizontalAlignment="Right" 
                       VerticalAlignment="Top"/>
    </Grid>

</UserControl>
