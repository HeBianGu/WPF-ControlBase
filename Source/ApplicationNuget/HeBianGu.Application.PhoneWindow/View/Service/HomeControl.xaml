<UserControl x:Class="HeBianGu.Application.PhoneWindow.View.Service.HomeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HeBianGu.Application.PhoneWindow.View.Service" xmlns:h="https://github.com/HeBianGu"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource {x:Static h:ButtonKeys.Transparent}}">
            <Setter Property="h:Cattach.Orientation" Value="Vertical"/>
            <Setter Property="h:Cattach.IconSize" Value="20"/>
            <Setter Property="h:Cattach.Icon" Value="&#xe7fd;"/>
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="Height" Value="Auto"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <DockPanel>
            <Border DockPanel.Dock="Top" Background="{DynamicResource {x:Static h:BrushKeys.Accent}}" Height="{DynamicResource {x:Static h:SystemKeys.RowHeight}}" CornerRadius="{DynamicResource {x:Static h:CornerRadiusKeys.Top}}">
                <TextBlock Text="服务" FontSize="{DynamicResource {x:Static h:FontSizeKeys.Header4}}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource {x:Static h:BrushKeys.ForegroundWhite}}"/>
            </Border>
            <ScrollViewer Margin="2,0">
                <ItemsControl>
                    <GroupBox Header="申报信息查询" Style="{StaticResource {x:Static h:GroupBoxKeys.UnderLine}}">
                        <UniformGrid Columns="3" Margin="0,10">
                            <Button Content="申报查询"/>

                            <Button Content="专项附加扣除信息查询"/>

                            <Button Content="收入纳税明细查询"/>

                        </UniformGrid>
                    </GroupBox>

                    <GroupBox Header="备案信息查询" Style="{StaticResource {x:Static h:GroupBoxKeys.UnderLine}}">
                        <UniformGrid Columns="3" Margin="0,10">
                            <Button Content="天使投资人个人所得税抵扣备案查询"/>

                            <Button Content="非货币性资产投资分期缴纳备案查询"/>

                            <Button Content="税收优惠备案查询"/>

                        </UniformGrid>
                    </GroupBox>

                    <GroupBox Header="其他查询" Style="{StaticResource {x:Static h:GroupBoxKeys.UnderLine}}">
                        <UniformGrid Columns="3" Margin="0,10">
                            <Button Content="异议处理查询"/>
                            <Button Content="税务文书查询"/>
                            <Button Content="涉税专业服务机构查询"/>
                        </UniformGrid>
                    </GroupBox>

                    <GroupBox Header="公众服务" Style="{StaticResource {x:Static h:GroupBoxKeys.UnderLine}}">
                        <UniformGrid Columns="3" Margin="0,10">
                            <Button Content="票证查验"/>
                            <Button Content="通知公告"/>
                            <Button Content="税收政策解读"/>
                            <Button Content="办税指南"/>
                            <Button Content="热点问题"/>
                        </UniformGrid>
                    </GroupBox>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>
