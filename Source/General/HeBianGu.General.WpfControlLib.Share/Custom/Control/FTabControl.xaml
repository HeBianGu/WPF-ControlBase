<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:h="QQ:908293466"
                    xmlns:local="clr-namespace:HeBianGu.General.WpfControlLib">

    <ContextMenu x:Key="S.FTabItemMenu.Default" x:Shared="False">
        <MenuItem Command="local:CommandService.Close" Header="关闭" />
        <MenuItem Command="local:CommandService.CloseAll" Header="全部关闭" />
        <MenuItem Command="local:CommandService.CloseOther" Header="除此之外全部关闭" />
    </ContextMenu>

    <Style x:Key="S.FTabItem.Default" TargetType="local:FTabItem">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.ForegroundDefault}}" />
        <Setter Property="Background" Value="{DynamicResource {x:Static h:BrushKeys.BackgroundDefault}}" />
        <Setter Property="BorderThickness" Value="1,1,1,1" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="16,0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Menu" Value="{StaticResource S.FTabItemMenu.Default}" />
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Background="Transparent" Text="{Binding Header, RelativeSource={RelativeSource AncestorType=local:FTabItem}}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:FTabItem">
                    <Grid x:Name="templateRoot" ContextMenu="{TemplateBinding Menu}" SnapsToDevicePixels="true">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border x:Name="mainBorder"
                                Grid.ColumnSpan="2"
                                Margin="0"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Border x:Name="innerBorder"
                                    Margin="0,0,0,-1"
                                    Background="{DynamicResource {x:Static h:BrushKeys.BackgroundDefault}}"
                                    Opacity="0" />
                        </Border>

                        <ContentPresenter x:Name="contentPresenter"
                                          Grid.Column="0"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Stretch"
                                          VerticalAlignment="Center"
                                          ContentSource="Header"
                                          Focusable="False"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                            <!--<ContentPresenter.OpacityMask>
                                <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                                    <GradientStop Offset="1"/>
                                    <GradientStop Color="White" Offset="0.8"/>
                                </LinearGradientBrush>
                            </ContentPresenter.OpacityMask>-->
                        </ContentPresenter>

                        <Button Grid.Column="1"
                                       h:Cattach.AllowsAnimation="True"
                                       Command="local:CommandService.Close"
                                       h:Cattach.IconSize="12"
                                       h:Cattach.Icon="&#xe6f4;"
                                       Style="{DynamicResource {x:Static h:ButtonKeys.Transparent}}"
                                       Visibility="{TemplateBinding ShowCloseButton,
                                                                    Converter={x:Static h:XConverter.BooleanToVisibilityConverter}}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter Property="Panel.ZIndex" Value="1" />
                            <Setter TargetName="innerBorder" Property="Opacity" Value="1" />
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.Accent}}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.AccentMouseOverForeground}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource S.FTabItem.Default}" TargetType="local:FTabItem" />

    <ControlTemplate x:Key="S.TabControlTemplate.Plus" TargetType="local:FTabControl">
        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <Grid x:Name="templateRoot"
                  ClipToBounds="true"
                  KeyboardNavigation.TabNavigation="Local"
                  SnapsToDevicePixels="true">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="ColumnDefinition0" />
                    <ColumnDefinition x:Name="ColumnDefinition1" Width="0" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Background="{TemplateBinding Background}" BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}" BorderThickness="0,0,0,1">
                    <local:FTabPanel x:Name="PART_HeaderPanel"
                                     MinHeight="{TemplateBinding TabItemHeight}"
                                     Margin="0,0,-1,-1"
                                     HorizontalAlignment="Left"
                                     Panel.ZIndex="1"
                                     Background="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                                     IsEnableTabFill="{TemplateBinding IsEnableTabFill}"
                                     IsItemsHost="true"
                                     KeyboardNavigation.TabIndex="1"
                                     TabItemHeight="{TemplateBinding TabItemHeight}"
                                     TabItemWidth="{TemplateBinding TabItemWidth}">
                        <h:Interaction.Behaviors>
                            <h:FluidMoveBehavior AppliesTo="Children" Duration="{Binding FluidMoveDuration, ElementName=PART_HeaderPanel}">
                                <h:FluidMoveBehavior.EaseX>
                                    <CubicEase EasingMode="EaseOut" />
                                </h:FluidMoveBehavior.EaseX>
                            </h:FluidMoveBehavior>
                        </h:Interaction.Behaviors>
                    </local:FTabPanel>
                </Border>
                <Border x:Name="contentPanel"
                        Grid.Row="1"
                        Grid.Column="0"
                        Background="{DynamicResource {x:Static h:BrushKeys.BackgroundDefault}}"
                        KeyboardNavigation.DirectionalNavigation="Contained"
                        KeyboardNavigation.TabIndex="2"
                        KeyboardNavigation.TabNavigation="Local">
                    <ContentPresenter x:Name="PART_SelectedContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      ContentSource="SelectedContent"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                </Border>
                <Border Grid.Row="0"
                        Grid.Column="0"
                        Margin="0,-1,0,0"
                        HorizontalAlignment="Right"
                        Background="{DynamicResource {x:Static h:BrushKeys.BackgroundDefault}}"
                        BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                        BorderThickness="1,0,0,1"
                        Visibility="{Binding Visibility, ElementName=PART_OverflowButton}">
                    <local:ContextMenuToggleButton x:Name="PART_OverflowButton"
                                                   Width="{DynamicResource S.Window.ItemHeight}"
                                                   Margin="5,0"
                                                   VerticalAlignment="Center"
                                                   h:Cattach.AllowsAnimation="{Binding ElementName=PART_OverflowButton, Path=IsChecked}"
                                                   h:Cattach.IconSize="15"
                                                   h:Cattach.Icon="&#xe9bb;"
                                                   Style="{DynamicResource {x:Static h:ToggleButtonKeys.BrushFIconChecked}}"
                                                   Visibility="Collapsed" />
                </Border>
            </Grid>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="S.TabControlTemplate.PlusScrollable" TargetType="local:FTabControl">
        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <Grid x:Name="templateRoot"
                  ClipToBounds="true"
                  KeyboardNavigation.TabNavigation="Local"
                  SnapsToDevicePixels="true">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Border Grid.Row="0"
                        Grid.Column="0"
                        Margin="0,-1,0,0"
                        HorizontalAlignment="Left"
                        Panel.ZIndex="2"
                        Background="{DynamicResource {x:Static h:BrushKeys.BackgroundDefault}}"
                        BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                        BorderThickness="0,0,1,1"
                        Visibility="{TemplateBinding ShowScrollButton,
                                                     Converter={x:Static h:XConverter.BooleanToVisibilityConverter}}">
                    <Border.CacheMode>
                        <BitmapCache SnapsToDevicePixels="True" />
                    </Border.CacheMode>
                    <RepeatButton x:Name="PART_ScrollButtonLeft" Padding="8" Focusable="False" />
                </Border>
                <local:FScrollViewer x:Name="PART_OverflowScrollviewer"
                                     Grid.Row="0"
                                     Grid.Column="1"
                                     HorizontalScrollBarVisibility="Hidden"
                                     IsDeferredScrollingEnabled="True"
                                     Style="{x:Null}">
                    <Border Name="PART_HeaderBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                            BorderThickness="0,0,0,1">
                        <local:FTabPanel x:Name="PART_HeaderPanel"
                                         MinHeight="{TemplateBinding TabItemHeight}"
                                         Margin="0,0,-1,-1"
                                         HorizontalAlignment="Left"
                                         Panel.ZIndex="1"
                                         Background="{DynamicResource SecondaryTextBrush}"
                                         IsEnableTabFill="{TemplateBinding IsEnableTabFill}"
                                         IsItemsHost="true"
                                         KeyboardNavigation.TabIndex="1"
                                         TabItemHeight="{TemplateBinding TabItemHeight}"
                                         TabItemWidth="{TemplateBinding TabItemWidth}">
                            <h:Interaction.Behaviors>
                                <h:FluidMoveBehavior AppliesTo="Children" Duration="{Binding FluidMoveDuration, ElementName=PART_HeaderPanel}">
                                    <h:FluidMoveBehavior.EaseX>
                                        <CubicEase EasingMode="EaseOut" />
                                    </h:FluidMoveBehavior.EaseX>
                                </h:FluidMoveBehavior>
                            </h:Interaction.Behaviors>
                        </local:FTabPanel>
                    </Border>
                </local:FScrollViewer>
                <Border Grid.Row="0"
                        Grid.Column="2"
                        Margin="0,-1,0,0"
                        HorizontalAlignment="Right"
                        Background="{DynamicResource {x:Static h:BrushKeys.BackgroundDefault}}"
                        BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                        BorderThickness="1,0,0,1">
                    <Border.CacheMode>
                        <BitmapCache SnapsToDevicePixels="True" />
                    </Border.CacheMode>
                    <StackPanel Orientation="Horizontal">
                        <RepeatButton x:Name="PART_ScrollButtonRight"
                                      Padding="8"
                                      Focusable="False"
                                      Visibility="{TemplateBinding ShowScrollButton,
                                                                   Converter={x:Static h:XConverter.BooleanToVisibilityConverter}}" />
                        <Border Width="1" Background="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}" Visibility="{Binding Visibility, ElementName=PART_OverflowButton}" />
                        <local:ContextMenuToggleButton x:Name="PART_OverflowButton" Visibility="Collapsed" />
                        <!--  Style="{StaticResource ToggleButtonIconTransparent}"  -->
                    </StackPanel>
                </Border>
                <Border x:Name="contentPanel"
                        Grid.Row="1"
                        Grid.Column="0"
                        Grid.ColumnSpan="3"
                        Background="{DynamicResource {x:Static h:BrushKeys.BackgroundDefault}}"
                        KeyboardNavigation.DirectionalNavigation="Contained"
                        KeyboardNavigation.TabIndex="2"
                        KeyboardNavigation.TabNavigation="Local">
                    <ContentPresenter x:Name="PART_SelectedContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      ContentSource="SelectedContent"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                </Border>
            </Grid>
        </Border>
    </ControlTemplate>

    <Style TargetType="local:FTabControl">
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="{DynamicResource {x:Static h:BrushKeys.BackgroundDefault}}" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="TabItemHeight" Value="{DynamicResource {x:Static h:SystemKeys.ItemHeight}}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template" Value="{StaticResource S.TabControlTemplate.Plus}" />
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsScrollable" Value="True" />
                    <Condition Property="IsEnableTabFill" Value="False" />
                </MultiTrigger.Conditions>
                <Setter Property="Template" Value="{StaticResource S.TabControlTemplate.PlusScrollable}" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <!--<Style BasedOn="{StaticResource TabControlBaseStyle}" TargetType="Local:FTabControl"/>-->

    <Style x:Key="TabItemInLine" BasedOn="{StaticResource S.FTabItem.Default}" TargetType="TabItem">
        <Setter Property="MinWidth" Value="0" />
    </Style>

    <Style x:Key="TabControlInLine" BasedOn="{StaticResource S.FTabItem.Default}" TargetType="TabControl">
        <Setter Property="ItemContainerStyle" Value="{StaticResource TabItemInLine}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid x:Name="templateRoot"
                              Background="{TemplateBinding Background}"
                              ClipToBounds="true"
                              KeyboardNavigation.TabNavigation="Local"
                              SnapsToDevicePixels="true">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="ColumnDefinition0" />
                                <ColumnDefinition x:Name="ColumnDefinition1" Width="0" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition x:Name="RowDefinition0" Height="Auto" />
                                <RowDefinition x:Name="RowDefinition1" Height="*" />
                            </Grid.RowDefinitions>
                            <UniformGrid x:Name="headerPanel"
                                         Grid.Row="0"
                                         Grid.Column="0"
                                         Margin="0"
                                         Panel.ZIndex="1"
                                         Background="Transparent"
                                         IsItemsHost="true"
                                         KeyboardNavigation.TabIndex="1"
                                         Rows="1" />
                            <Border x:Name="contentPanel"
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                                    BorderThickness="0,1,0,0"
                                    KeyboardNavigation.DirectionalNavigation="Contained"
                                    KeyboardNavigation.TabIndex="2"
                                    KeyboardNavigation.TabNavigation="Local">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  Margin="0"
                                                  ContentSource="SelectedContent"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Border>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="TabStripPlacement" Value="Bottom">
                            <Setter TargetName="RowDefinition0" Property="Height" Value="*" />
                            <Setter TargetName="RowDefinition1" Property="Height" Value="Auto" />
                            <Setter TargetName="headerPanel" Property="Grid.Row" Value="1" />
                            <Setter TargetName="contentPanel" Property="Grid.Row" Value="0" />
                            <Setter TargetName="contentPanel" Property="BorderThickness" Value="0,0,0,1" />
                        </Trigger>
                        <Trigger Property="TabStripPlacement" Value="Left">
                            <Setter TargetName="RowDefinition0" Property="Height" Value="*" />
                            <Setter TargetName="RowDefinition1" Property="Height" Value="0" />
                            <Setter TargetName="ColumnDefinition0" Property="Width" Value="Auto" />
                            <Setter TargetName="ColumnDefinition1" Property="Width" Value="*" />
                            <Setter TargetName="contentPanel" Property="Grid.Column" Value="1" />
                            <Setter TargetName="contentPanel" Property="Grid.Row" Value="0" />
                            <Setter TargetName="contentPanel" Property="BorderThickness" Value="1,0,0,0" />
                            <Setter TargetName="headerPanel" Property="Grid.Column" Value="0" />
                            <Setter TargetName="headerPanel" Property="Grid.Row" Value="0" />
                            <Setter TargetName="headerPanel" Property="Rows" Value="0" />
                            <Setter TargetName="headerPanel" Property="Columns" Value="1" />
                        </Trigger>
                        <Trigger Property="TabStripPlacement" Value="Right">
                            <Setter TargetName="RowDefinition0" Property="Height" Value="*" />
                            <Setter TargetName="RowDefinition1" Property="Height" Value="0" />
                            <Setter TargetName="ColumnDefinition0" Property="Width" Value="*" />
                            <Setter TargetName="ColumnDefinition1" Property="Width" Value="Auto" />
                            <Setter TargetName="contentPanel" Property="Grid.Column" Value="0" />
                            <Setter TargetName="contentPanel" Property="Grid.Row" Value="0" />
                            <Setter TargetName="contentPanel" Property="BorderThickness" Value="0,0,1,0" />
                            <Setter TargetName="headerPanel" Property="Grid.Column" Value="1" />
                            <Setter TargetName="headerPanel" Property="Grid.Row" Value="0" />
                            <Setter TargetName="headerPanel" Property="Rows" Value="0" />
                            <Setter TargetName="headerPanel" Property="Columns" Value="1" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TabItemCapsuleDefault" BasedOn="{StaticResource S.FTabItem.Default}" TargetType="TabItem" />

    <!--<Style x:Key="TabItemCapsuleHorizontalFirst" BasedOn="{StaticResource TabItemCapsuleBaseStyle}" TargetType="TabItem">
        <Setter Property="local:BorderElement.CornerRadius" Value="4,0,0,4"/>
        <Setter Property="Margin" Value="0"/>
    </Style>

    <Style x:Key="TabItemCapsuleHorizontalLast" BasedOn="{StaticResource TabItemCapsuleBaseStyle}" TargetType="TabItem">
        <Setter Property="local:BorderElement.CornerRadius" Value="0,4,4,0"/>
    </Style>-->

    <!--<Style x:Key="TabItemCapsuleSingle" BasedOn="{StaticResource TabItemCapsuleBaseStyle}" TargetType="TabItem">
        <Setter Property="local:BorderElement.CornerRadius" Value="{StaticResource DefaultCornerRadius}"/>
    </Style>

    <Style x:Key="TabItemCapsuleVerticalFirst" BasedOn="{StaticResource TabItemCapsuleBaseStyle}" TargetType="TabItem">
        <Setter Property="local:BorderElement.CornerRadius" Value="4,4,0,0"/>
        <Setter Property="Margin" Value="0"/>
    </Style>-->

    <!--<Style x:Key="TabItemCapsuleVerticalLast" BasedOn="{StaticResource TabItemCapsuleBaseStyle}" TargetType="TabItem">
        <Setter Property="local:BorderElement.CornerRadius" Value="0,0,4,4"/>
    </Style>-->

    <Style x:Key="TabControlCapsule" TargetType="TabControl">
        <Setter Property="Background" Value="{DynamicResource {x:Static h:BrushKeys.BackgroundDefault}}" />
        <!--<Setter Property="local:TitleElement.BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="local:TitleElement.Background" Value="{DynamicResource {x:Static h:BrushKeys.BackgroundDefault}}"/>
        <Setter Property="local:TitleElement.Foreground" Value="{DynamicResource PrimaryBrush}"/>-->
        <!--<Setter Property="ItemContainerStyleSelector">
            <Setter.Value>
                <tools:TabItemCapsuleStyleSelector/>
            </Setter.Value>
        </Setter>-->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid x:Name="templateRoot"
                          Background="{TemplateBinding Background}"
                          ClipToBounds="true"
                          KeyboardNavigation.TabNavigation="Local"
                          SnapsToDevicePixels="true">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="ColumnDefinition0" />
                            <ColumnDefinition x:Name="ColumnDefinition1" Width="0" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition x:Name="RowDefinition0" Height="Auto" />
                            <RowDefinition x:Name="RowDefinition1" Height="*" />
                        </Grid.RowDefinitions>
                        <UniformGrid x:Name="headerPanel"
                                     Grid.Row="0"
                                     Grid.Column="0"
                                     Margin="0"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Panel.ZIndex="1"
                                     Background="Transparent"
                                     IsItemsHost="true"
                                     KeyboardNavigation.TabIndex="1"
                                     Rows="1" />
                        <Border x:Name="contentPanel"
                                Grid.Row="1"
                                Grid.Column="0"
                                Margin="0,6,0,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource {x:Static h:BrushKeys.BorderBrushDefault}}"
                                BorderThickness="1"
                                KeyboardNavigation.DirectionalNavigation="Contained"
                                KeyboardNavigation.TabIndex="2"
                                KeyboardNavigation.TabNavigation="Local">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              Margin="0"
                                              ContentSource="SelectedContent"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="TabStripPlacement" Value="Bottom">
                            <Setter TargetName="RowDefinition0" Property="Height" Value="*" />
                            <Setter TargetName="RowDefinition1" Property="Height" Value="Auto" />
                            <Setter TargetName="headerPanel" Property="Grid.Row" Value="1" />
                            <Setter TargetName="contentPanel" Property="Margin" Value="0,0,0,6" />
                            <Setter TargetName="contentPanel" Property="Grid.Row" Value="0" />
                        </Trigger>
                        <Trigger Property="TabStripPlacement" Value="Left">
                            <Setter TargetName="RowDefinition0" Property="Height" Value="*" />
                            <Setter TargetName="RowDefinition1" Property="Height" Value="0" />
                            <Setter TargetName="ColumnDefinition0" Property="Width" Value="Auto" />
                            <Setter TargetName="ColumnDefinition1" Property="Width" Value="*" />
                            <Setter TargetName="headerPanel" Property="Grid.Column" Value="0" />
                            <Setter TargetName="headerPanel" Property="Grid.Row" Value="0" />
                            <Setter TargetName="headerPanel" Property="Rows" Value="0" />
                            <Setter TargetName="headerPanel" Property="Columns" Value="1" />
                            <Setter TargetName="contentPanel" Property="Grid.Column" Value="1" />
                            <Setter TargetName="contentPanel" Property="Grid.Row" Value="0" />
                            <Setter TargetName="contentPanel" Property="Margin" Value="6,0,0,0" />
                        </Trigger>
                        <Trigger Property="TabStripPlacement" Value="Right">
                            <Setter TargetName="RowDefinition0" Property="Height" Value="*" />
                            <Setter TargetName="RowDefinition1" Property="Height" Value="0" />
                            <Setter TargetName="ColumnDefinition0" Property="Width" Value="*" />
                            <Setter TargetName="ColumnDefinition1" Property="Width" Value="Auto" />
                            <Setter TargetName="headerPanel" Property="Grid.Column" Value="1" />
                            <Setter TargetName="headerPanel" Property="Grid.Row" Value="0" />
                            <Setter TargetName="headerPanel" Property="Rows" Value="0" />
                            <Setter TargetName="headerPanel" Property="Columns" Value="1" />
                            <Setter TargetName="contentPanel" Property="Grid.Column" Value="0" />
                            <Setter TargetName="contentPanel" Property="Grid.Row" Value="0" />
                            <Setter TargetName="contentPanel" Property="Margin" Value="0,0,6,0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TabControlCapsuleSolid" BasedOn="{StaticResource TabControlCapsule}" TargetType="TabControl">
        <!--<Setter Property="local:TitleElement.BorderBrush" Value="{DynamicResource TitleBrush}"/>
        <Setter Property="local:TitleElement.Background" Value="{DynamicResource TitleBrush}"/>
        <Setter Property="local:TitleElement.Foreground" Value="{DynamicResource TextIconBrush}"/>-->
    </Style>
</ResourceDictionary>
