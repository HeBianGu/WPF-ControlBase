<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:h="QQ:908293466"
                    xmlns:local="clr-namespace:HeBianGu.General.WpfControlLib">

    <Style x:Key="S.FGrid.Default" TargetType="{x:Type local:FGrid}">
        <!--<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        -->
        <!--  内容背景色  -->
        <!--
        <Setter Property="Background" Value="{DynamicResource {x:Static h:BrushKeys.LightGray}}"/>
        -->
        <!--<Setter Property="Foreground" Value="{DynamicResource {x:Static h:BrushKeys.White}}"/>-->
        <!--
        <Setter Property="MinWidth" Value="180"/>
        <Setter Property="CanHide" Value="True"/>
        <Setter Property="IsExpanded" Value="False"/>
        <Setter Property="Hint" Value="{x:Null}"/>-->
        <!--  详细信息颜色  -->
        <!--<Setter Property="HintBackground" Value="{DynamicResource {x:Static h:BrushKeys.Accent}}"/>
        <Setter Property="HintForeground" Value="{DynamicResource {x:Static h:BrushKeys.White}}"/>-->


    </Style>

    <Style x:Key="S.FGrid.MouseDragElementBehavior" BasedOn="{StaticResource S.FGrid.Default}" TargetType="{x:Type local:FGrid}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:FGrid}">
                    <Grid>
                        <h:Interaction.Behaviors>
                            <h:MouseDragElementBehavior />
                        </h:Interaction.Behaviors>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
