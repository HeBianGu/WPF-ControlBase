<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:h="https://github.com/HeBianGu">

    <!--<Style TargetType="{x:Type RibbonTab}">
        <Setter Property="Header" Value="{Binding Label}" />
        <Setter Property="Visibility" Value="{Binding IsVisible, Converter={x:Static h:XConverter.BooleanToVisibilityConverter}}" />
        <Setter Property="ItemsSource" Value="{Binding GroupDataCollection}" />
    -->
    <!--<Setter Property="ItemTemplate">
            <Setter.Value>
                <HierarchicalDataTemplate DataType="{x:Type RibbonGroup}" ItemsSource="{Binding ControlDataCollection}">
                    <TextBlock Text="{Binding Label}" Visibility="{Binding IsVisible, Converter={x:Static h:XConverter.BooleanToVisibilityConverter}}" />
                </HierarchicalDataTemplate>
            </Setter.Value>
        </Setter>-->
    <!--
    </Style>-->
</ResourceDictionary>