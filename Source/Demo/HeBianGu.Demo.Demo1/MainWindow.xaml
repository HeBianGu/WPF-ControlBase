<Window x:Class="HeBianGu.Demo.Demo1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:h="QQ:908293466"
        xmlns:local="clr-namespace:HeBianGu.Demo.Demo1"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        Title="最简单的使用方式"
        Width="800"
        Height="450"
        mc:Ignorable="d">
    <UniformGrid>
        <TextBlock FontSize="{DynamicResource {x:Static h:FontSizeKeys.Header1}}" Style="{DynamicResource {x:Static h:TextBlockKeys.Default}}" Text="TextBlock" />

        <Button Content="Button" Style="{DynamicResource {x:Static h:ButtonKeys.Default}}" />

        <Button Content="Accent" Style="{DynamicResource {x:Static h:ButtonKeys.Accent}}" />

        <Button Content="Button1" />

        <CheckBox Style="{DynamicResource {x:Static h:CheckBoxKeys.Default}}" />

        <ComboBox Style="{DynamicResource {x:Static h:ComboBoxKeys.Default}}">
            <system:String>1</system:String>
            <system:String>2</system:String>
            <system:String>3</system:String>
            <system:String>4</system:String>
        </ComboBox>

        <DatePicker Style="{DynamicResource {x:Static h:DatePickerKeys.Default}}" />
    </UniformGrid>
</Window>
