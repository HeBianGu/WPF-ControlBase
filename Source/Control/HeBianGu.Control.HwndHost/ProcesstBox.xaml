<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:h="https://github.com/HeBianGu"
                    xmlns:local="clr-namespace:HeBianGu.Control.HwndHost">

    <Style TargetType="{x:Type local:ProcesstBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ProcesstBox}">
                    <Border Background="Orange" Margin="10">
                        <Grid>
                            <ContentPresenter Content="{TemplateBinding Content}"/>
                            <!--<local:OverlayControl HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Grid>
                                    <ProgressBar Background="Red" IsIndeterminate="True" Width="200" Height="45" Style="{DynamicResource {x:Type ProgressBar}}"/>
                                    <Button Content="111"/>
                                </Grid>
                            </local:OverlayControl>-->

                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

