<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:HeBianGu.Control.Chart2D" 
                    xmlns:system="clr-namespace:System;assembly=mscorlib">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="LayerGroup.xaml"/>
        <ResourceDictionary Source="Line.xaml"/>
        <ResourceDictionary Source="Scatter.xaml"/>
        <ResourceDictionary Source="Bar.xaml"/>
        <ResourceDictionary Source="../Marker/Marker.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="S.Series.Default" TargetType="{x:Type local:Series}" BasedOn="{StaticResource S.LayerGroup.Default}"/>

    <Style TargetType="{x:Type local:Series}" BasedOn="{StaticResource S.Series.Default}"/>

    <Style x:Key="S.Series.Line.Basic" TargetType="{x:Type local:Series}" BasedOn="{StaticResource S.Series.Default}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Series}">
                    <local:LayerGroup>
                        <local:Line Foreground="{TemplateBinding Foreground}" Data="{TemplateBinding Data}" />

                        <local:Scatter Foreground="{TemplateBinding Foreground}" MarkStyle="{StaticResource S.EllipseMarker.4}" Data="{TemplateBinding Data}"/>

                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </local:LayerGroup>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="S.Series.Line.Area" TargetType="{x:Type local:Series}" BasedOn="{StaticResource S.Series.Default}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Series}">
                    <local:LayerGroup>
                        <local:Area  Foreground="{TemplateBinding Foreground}" Data="{TemplateBinding Data}" />

                        <local:Scatter Foreground="{TemplateBinding Foreground}" MarkStyle="{StaticResource S.EllipseMarker.4}" Data="{TemplateBinding Data}"/>

                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </local:LayerGroup>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="S.Series.Bar.Basic" TargetType="{x:Type local:Series}" BasedOn="{StaticResource S.Series.Default}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Series}">
                    <local:LayerGroup>
                        
                        <local:Bar Foreground="{TemplateBinding Foreground}" MarkStyle="{StaticResource S.EllipseMarker.4}" Data="{TemplateBinding Data}"/>

                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </local:LayerGroup>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

