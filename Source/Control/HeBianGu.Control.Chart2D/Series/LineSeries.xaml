<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:HeBianGu.Control.Chart2D" 
                    xmlns:system="clr-namespace:System;assembly=mscorlib">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Series.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="S.LineSeries.Default" TargetType="{x:Type local:LineSeries}" BasedOn="{StaticResource S.Series.Default}">
        <Setter Property="PathStyle">
            <Setter.Value>
                <Style TargetType="Path">
                    <Setter Property="StrokeThickness" Value="1"/>
                    <Setter Property="Stroke" Value="{Binding RelativeSource={RelativeSource AncestorType=local:Series},Path=Foreground}"/>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="{x:Type local:LineSeries}" BasedOn="{StaticResource S.LineSeries.Default}"/>

    <Style x:Key="S.AreaSeries.Default" TargetType="{x:Type local:AreaSeries}" BasedOn="{StaticResource S.Series.Default}">
        <Setter Property="PathStyle">
            <Setter.Value>
                <Style TargetType="Path">
                    <Setter Property="Fill" Value="{Binding RelativeSource={RelativeSource AncestorType=local:Series},Path=Foreground}"/>
                    <Setter Property="StrokeThickness" Value="1"/>
                    <Setter Property="Stroke" Value="{Binding RelativeSource={RelativeSource AncestorType=local:Series},Path=Foreground}"/>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:AreaSeries}" BasedOn="{StaticResource S.AreaSeries.Default}"/>
</ResourceDictionary>

