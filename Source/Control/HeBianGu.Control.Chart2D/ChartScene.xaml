<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:HeBianGu.Control.Chart2D" 
                    xmlns:system="clr-namespace:System;assembly=mscorlib">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Scene.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <!--默认样式 需要自己添加坐标系等元素 -->
    <Style x:Key="S.ChartScene.Default" TargetType="{x:Type local:ChartScene}" BasedOn="{StaticResource S.Scene.Default}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ChartScene}">
                    <local:Scene>
                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </local:Scene>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--带有直角坐标系的图-->
    <Style x:Key="S.ChartScene.Coord" TargetType="{x:Type local:ChartScene}" BasedOn="{StaticResource S.ChartScene.Default}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ChartScene}">
                    <local:Scene>
                        <local:GridLayer/>

                        <local:xAxisLayer/>

                        <local:yAxisLayer/>

                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                    </local:Scene>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="{x:Type local:ChartScene}" BasedOn="{StaticResource S.ChartScene.Default}"/>

</ResourceDictionary>

