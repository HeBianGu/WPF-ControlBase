<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:HeBianGu.Control.Chart2D" 
                    xmlns:system="clr-namespace:System;assembly=mscorlib">


    <Style x:Key="S.Axis.Default" TargetType="{x:Type local:AxisLayer}">
        <Setter Property="Margin" Value="{Binding RelativeSource={RelativeSource AncestorType=local:ChartScene},Path=Padding}"/>
        <Setter Property="xAxis" Value="{Binding RelativeSource={RelativeSource AncestorType=local:ChartScene},Path=xAxis}"/>
        <Setter Property="yAxis" Value="{Binding RelativeSource={RelativeSource AncestorType=local:ChartScene},Path=yAxis}"/>
        <Setter Property="LineLenght" Value="5"/>
        <Setter Property="LineStyle">
            <Setter.Value>
                <Style TargetType="Line">
                    <Setter Property="StrokeThickness" Value="2"/>
                    <Setter Property="Stroke" Value="{DynamicResource S.Brush.Black.Notice}"/>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="LabelStyle">
            <Setter.Value>
                <Style TargetType="Label">
                    <Setter Property="Foreground" Value="{DynamicResource S.Brush.Black.Notice}"/>
                    <Setter Property="FontSize" Value="15"/>
                    <!--<Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="Auto"/>-->
                    <!--<Setter Property="Background" Value="Red"/>-->
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="S.xAxis.Default" TargetType="{x:Type local:xAxisLayer}" BasedOn="{StaticResource S.Axis.Default}">
        <Setter Property="VerticalAlignment" Value="Bottom"/>
    </Style>

    <Style x:Key="S.yAxis.Default" TargetType="{x:Type local:yAxisLayer}" BasedOn="{StaticResource S.Axis.Default}">
        <Setter Property="HorizontalAlignment" Value="Left"/>
    </Style>

    <Style TargetType="{x:Type local:yAxisLayer}" BasedOn="{StaticResource S.yAxis.Default}"/>


    <Style TargetType="{x:Type local:xAxisLayer}" BasedOn="{StaticResource S.xAxis.Default}"/>

</ResourceDictionary>

